{
  "name": "palmr",
  "private": true,
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "build:release": "pnpm --filter web build && cargo build --release --locked --package palmr-server --bin palmr",
    "api:generate": "./scripts/generate-api-types.sh"
  }
}
